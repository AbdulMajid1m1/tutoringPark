<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>whatsapp icon</title>

    <style type="text/css">
      .whatapp-wrapper {
        display: flex;
        position: fixed;
        bottom: 20px;
        right: 20px;
        z-index: 100;
        transition: all 0.3s ease;
        align-items: center;
      }
      .float {
        width: 50px;
        height: 50px;
        background-color: #25d366;
        color: #fff;
        border-radius: 50%;
        text-align: center;
        font-size: 35px;
        z-index: 100;
        position: relative;
        animation: size 1s infinite 1s;
        margin-left: 10px;
        /* animation: ripple 1s linear infinite; */

        /* transition: all 0.7s ease; */
        /* display: flex;
        justify-content: center;
        align-items: center; */
      }
      .my-float {
        margin-top: 8px;
      }

      .float:before,
      .float:after {
        content: "";
        display: block;
        position: absolute;
        top: 0;
        right: 0;
        bottom: 0;
        left: 0;
        /* border: 1px solid #999; */
        border: 1px solid rgba(0, 0, 0, 0.2);
        border-radius: 50%;
        transition: all 0.5s ease;
      }

      .float:before {
        animation: ripple 1s linear infinite;
      }

      .float:after {
        animation: ripple 3s linear 2s infinite;
      }
      @keyframes ripple {
        0% {
          transform: scale(1);
          opacity: 0.3;
          border: 2px solid rgba(23, 19, 19, 0.1);
        }
        50% {
          transform: scale(1.5);
          opacity: 0.5;
          border: 5px solid rgba(23, 19, 19, 0.1);
        }
        100% {
          transform: scale(1.7);
          opacity: 0.3;
          border: 6px solid rgba(23, 19, 19, 0.1);
        }
      }
      @keyframes size {
        0% {
          transform: scale(1);
        }
        50% {
          transform: scale(1.05);
        }
        100% {
          transform: scale(1.08);
        }
      }

      .tooltip-text {
        /* visibility: hidden; */
        position: absolute;

        z-index: 2;
        width: max-content;
        color: black;
        font-size: 0.9rem;
        font-weight: 600;
        font-style: italic;
        background-color: white;
        border-radius: 20px;
        padding: 5px 15px 5px 15px;
        box-shadow: rgba(0, 0, 0, 0.16) 0px 1px 4px;
      }

      .tooltip-text::before {
        content: "";
        position: absolute;
        transform: rotate(45deg);
        background-color: white;
        padding: 4px 5px;
        z-index: 1;
        /* box-shadow: rgba(0, 0, 0, 0.16) 0px 0px 2px; */
        box-shadow: rgba(0, 0, 0, 0.15) 1.95px 0px 2.6px;
      }
      .blink-carot {
        /* content: ""; */
        width: 3px;
        height: 28px;
        position: absolute;
        background: #ff802b;
        display: inline-block;
        animation: cursor-blink 0.7s steps(2) infinite;
      }
      @keyframes cursor-blink {
        0% {
          opacity: 0;
        }
      }
      #left {
        top: 8px;
        right: 120%;
      }

      #left::before {
        top: 35%;
        left: 96%;
      }
      .float:hover {
        color: white !important;
      }
      @media only screen and (max-width: 450px) {
        .tooltip-text {
          display: none;
        }
        .float {
          width: 40px;
          height: 40px;
          font-size: 25px;
        }
      }
    </style>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css"
    />
  </head>
  <body>
    <div class="whatapp-wrapper">
      <a
        href="https://api.whatsapp.com/send?phone=447883678668&text=Hi Sir!"
        target="_blank"
      >
        <span class="tooltip-text" id="left"
          >Hi, I am here to assist you.<span class="blink-carot"></span
        ></span>
        <a
          href="https://api.whatsapp.com/send?phone=447883678668&text=Hi Sir!"
          class="float"
          target="_blank"
        >
          <i class="fa fa-whatsapp my-float"></i>
        </a>
      </a>
    </div>
  </body>
</html>
